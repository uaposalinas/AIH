const express = require('express');
const app = express();
const Port = 4000;

app.use(express.static(path.join(__dirname, 'public')));

app.listen(Port, () => {

    console.log(`Servidor en escucha en el puerto ${Port} en la url http://localhost:${Port}`);

})